# mochios live session
if [ -z "$MOCHI_LIVE_INIT" ]; then
    export MOCHI_LIVE_INIT=1
    echo "welcome to mochios live!"
    echo "the installer should open shortly..."
    echo "if not, run: mochiinstall"
fi
